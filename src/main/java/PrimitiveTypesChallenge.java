public class PrimitiveTypesChallenge {

    public long sum(byte i, short ii, int iii) {

        // Declare left primitive values as longs using "L"
        // matches actual result employing casting for test case
        return (50000L + 10L * (i + ii + iii));
    }
}
